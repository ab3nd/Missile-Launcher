# Put this file in /etc/udev/rules.d/ to get permission to access the
# usb device files for the Dream Cheeky Missile Launcher.
# (This has been tested only on Debian GNU/Linux.)
#
# Replace "plugdev" with whatever group your distro defines for
# users that should be able to access pluggable devices.
# (or your own group if you only care about yourself, run groups
# to see which groups you are a member of)

ACTION!="add", GOTO="permissions_end"

SUBSYSTEM=="usb_device",                        GROUP="plugdev", \
        ATTRS{idVendor}=="1941", ATTRS{idProduct}=="8021"
SUBSYSTEM=="usb_device",                        GROUP="plugdev", \
        ATTRS{idVendor}=="0a81", ATTRS{idProduct}=="0701"

LABEL="permissions_end"
